# 🥧 파이 노드(Pi Node) 24시간 PC에 누리오(NURIOH) 서버 구축 가이드

> **비용 0원!** 이미 24시간 켜져 있는 파이 노드 운영 컴퓨터를 대표님만의 완벽한 24시간 365일 전용 트레이딩 서버로 활용하는 방법입니다.

---

## 🌟 전체 작업 흐름 요약 (10분 완성)
```
1. 파이 노드 PC에 Node.js 설치
2. 프로젝트 폴더 복사
3. 파이 노드 PC의 공인 IP를 업비트에 등록
4. PM2(24시간 무중단 자동 재시작 도구)로 서버 가동
5. 스마트폰 접속용 Cloudflare Tunnel 실행
```

---

## STEP 1. 파이 노드 PC에 Node.js 설치하기
1. 파이 노드 PC의 브라우저에서 [https://nodejs.org](https://nodejs.org) 접속.
2. **LTS 버전 (v20 또는 v22)** 다운로드 후 설치 (기본 설정대로 Next 클릭).

---

## STEP 2. 프로젝트 파일 가져오기
대표님의 현재 컴퓨터에 있는 `자동매매프로그램` 폴더를 파이 노드 PC로 가져옵니다.

- **방법 A (시놀로지 드라이브 / SynologyDrive 이용 - 추천):**
  - 파이 노드 PC에 Synology Drive 클라이언트를 설치하여 동일한 폴더를 동기화.
- **방법 B (USB 또는 압축 파일 복사):**
  - 현재 `y:\SynologyDrive\00.withAI\자동매매프로그램` 폴더 전체를 압축하여 USB나 이메일로 파이 노드 PC의 `C:\NURIOH_Trader` 폴더에 복사.

---

## STEP 3. 파이 노드 PC의 공인 IP를 업비트에 등록 (★핵심)
파이 노드 PC에서 실제 업비트 주문을 날리게 되므로, **파이 노드 PC의 인터넷 IP를 업비트에 등록**해야 합니다.

1. 파이 노드 PC에서 브라우저를 열고 [https://api.ipify.org](https://api.ipify.org) 에 접속하여 파이 노드 PC의 공인 IP 확인.
   - *(만약 현재 컴퓨터와 파이 노드 PC가 같은 집/사무실 공유기에 연결되어 있다면 현재 IP `49.171.41.10`와 동일합니다!)*
2. 업비트 > **[마이페이지] > [Open API 관리]** 에서 해당 IP가 등록되어 있는지 확인.

---

## STEP 4. 24시간 무중단 백그라운드 구동 (PM2 설치)
파이 노드 PC에서 터미널(PowerShell 또는 명령 프롬프트)을 열고 프로젝트 폴더로 이동한 뒤 실행합니다:

```bash
# 1. 프로젝트 폴더로 이동 (예시)
cd C:\NURIOH_Trader

# 2. 패키지 설치
npm install

# 3. 24시간 무중단 프로세스 관리 도구(PM2) 전역 설치
npm install -g pm2

# 4. 서버 무중단 가동
pm2 start server/index.js --name "nurioh-trader"

# 5. 컴퓨터 재부팅 시에도 자동 실행되도록 등록
pm2 startup
pm2 save
```

*(이제 파이 노드 PC를 재부팅해도 컴퓨터가 켜지자마자 프로그램이 백그라운드에서 자동으로 살아납니다!)*

---

## STEP 5. 스마트폰 외부 접속 연결 (Cloudflare Tunnel)
파이 노드 PC에서 아래 명령어를 실행하면, 외부 어디서든 스마트폰으로 접속할 수 있는 고유 링크가 생성됩니다:

```bash
npx cloudflared tunnel --url http://localhost:4000
```

---

## 🎯 세팅 완료 후 누리는 혜택!
1. **내 메인 컴퓨터는 언제든 꺼도 됨:** 작업 끝나면 메인 PC 끄고 주무셔도 됩니다.
2. **24시간 365일 실시간 감시:** 파이 노드 PC가 지치지 않고 24시간 시세를 감시하며, 신호 발생 시 스마트폰 텔레그램으로 승인 알림을 보냅니다.
3. **전기세/서버비 추가 0원:** 이미 켜져있는 파이 노드 PC를 활용하므로 추가 비용이 전혀 들지 않습니다!
