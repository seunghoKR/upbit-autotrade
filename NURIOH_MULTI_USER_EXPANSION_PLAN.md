# 🏢 누리오(NURIOH) 다중 회원 서비스(SaaS) 확장 개발 계획서

---

## 1. 프로젝트 비전 및 목표
- **목표:** 현재 1인용 시스템을 **다중 회원(지인, VIP 투자자, 일반 고객)이 각자의 업비트 계정으로 안전하게 자동매매를 이용할 수 있는 '비수탁형 멀티 테넌트 SaaS'**로 확장.
- **핵심 원칙:**
  - **자금 비예치 (Non-Custodial):** 회원의 자금을 절대 직접 받지 않고, 각자의 업비트 API 키를 통해 본인 계좌에서만 거래.
  - **철통 보안 (Data Isolation):** 회원별 API 키는 `AES-256`으로 분리 암호화되어 타인이나 해킹으로부터 안전하게 격리.
  - **개별 텔레그램 승인:** 각 회원은 **본인 텔레그램**으로만 매매 신호를 받아 본인 자금에 맞게 승인/취소.

```mermaid
flowchart TB
    subgraph Users [다중 회원 계층]
        UserA[회원 A (지인)]
        UserB[회원 B (VIP)]
        UserC[회원 C (일반)]
    end

    subgraph Platform [누리오(NURIOH) 클라우드 플랫폼]
        Auth[Firebase Auth\n(회원가입 / 로그인)]
        DB[(Cloud Firestore\n회원별 암호화 API Key & 설정)]
        Engine[중앙 전략 엔진\n(병렬 멀티 테넌트 워커)]
        AdminPanel[대표님 전용\n마스터 관리자 대시보드]
    end

    subgraph UpbitExchange [업비트 거래소]
        UpbitA[회원 A 업비트 계좌]
        UpbitB[회원 B 업비트 계좌]
        UpbitC[회원 C 업비트 계좌]
    end

    Users -->|1. 웹 로그인 & API키 등록| Auth
    Auth --> DB
    Engine -->|2. 시세 분석 & 개별 신호| Users
    Users -->|3. 개인 텔레그램 승인| Engine
    Engine -->|4. 개별 주문 집행| UpbitExchange
    AdminPanel -->|회원 관리 & 이용권 제어| DB
```

---

## 2. 회원 시스템 아키텍처 및 추가 필요 작업

### 2.1 회원 인증 및 마이페이지 (Firebase Auth)
- **간편 로그인:** 카카오톡 / 구글 / 이메일 원클릭 가입 및 로그인 지원.
- **개인 API 키 관리 페이지:** 회원이 직접 본인의 업비트 `Access Key` / `Secret Key`를 등록 및 테스트.
- **서버 고정 IP 안내:** "업비트에 서버 IP `XX.XX.XX.XX`를 등록해 주세요" 화면 자동 안내.

### 2.2 1:1 개인 텔레그램 봇 자동 연동
- **개인화 연동 링크:** 회원이 대시보드에서 `[내 텔레그램 연결하기]` 버튼을 누르면 봇과 자동 1:1 매핑 (Deep Linking).
- 매매 신호 발생 시 다른 회원과 섞이지 않고 **오직 해당 회원의 텔레그램으로만 알림 & 승인 버튼 발송**.

### 2.3 24시간 클라우드 백엔드 (Cloud Run / AWS VPS)
- 대표님 PC를 켜두지 않아도 **24시간 365일 무중단으로 작동하는 클라우드 고정 IP 서버** 구축.
- 회원 10명~1,000명이 동시에 접속해도 과부하 없는 비동기 이벤트 큐(Redis / BullMQ) 구조 적용.

### 2.4 대표님 전용 마스터 관리자 패널 (Super Admin)
- **회원 관리:** 가입 회원 목록, 승인 상태, 최근 접속일 조회.
- **이용권/구독 제어:** 무료 체험 기간(예: 7일/30일), 유료 정기 결제 플랜, 강제 일시정지 스위치.
- **통합 모니터링:** 전체 회원 총 거래액, 성공률, 시스템 서버 리소스 실시간 모니터링.

---

## 3. 회원 관리 및 운영 플로우 (어떻게 관리하나요?)

| 단계 | 사용자(회원) 관점 | 대표님(관리자) 관점 |
| :--- | :--- | :--- |
| **1. 초대 & 가입** | 대표님이 공유한 링크로 접속하여 카카오/구글 회원가입 | 신규 회원 가입 알림 수신 및 승인 권한 부여 |
| **2. API 키 등록** | 본인 업비트에서 발급한 API 키 등록 (출금권한 제외 안내) | 회원의 API 키 등록 여부 및 통신 정상 여부 확인 |
| **3. 텔레그램 연결** | 텔레그램 연동 버튼 터치하여 본인 메신저 연결 | 회원 텔레그램 ID 자동 맵핑 확인 |
| **4. 매매 진행** | 본인 텔레그램으로 오는 신호를 보고 승인/취소 매매 진행 | 전체 회원 매매 집행 로그 및 통계 모니터링 |
| **5. 기간 관리** | 이용권 만료 시 알림 수신 및 연장 신청 | 구독 기간 설정 및 만료 회원 자동 잠금 관리 |

---

## 4. 법적 요건 및 비즈니스 면책 전략 (Compliance)

1. **투자일임/자문 규제 회피 (합법적 비수탁형 SaaS):**
   - 대표님이 고객의 자금을 받아서 대신 굴려주는 형태(투자일임)가 아니라, **고객이 본인 계좌에서 직접 승인하여 실행하는 "소프트웨어 도구(Tool) 제공" 모델**로 구성.
   - 자본시장법상 유사투자자문업 신고 의무 여부를 확인하고, 맞춤형 1:1 리딩이 아닌 자동 지표 시그널 제공 소프트웨어임을 약관에 명시.
2. **필수 약관 및 동의서 구축:**
   - 회원 가입 시 **"본 프로그램은 투자 보조 도구이며, 최종 투자 판단과 손익 책임은 본인에게 있습니다"** 필수 체크 동의.

---

## 5. 단계별 개발 로드맵

- **Step 1 (회원 계정 & DB 분리):** Firebase Auth 가입 + Cloud Firestore 회원별 암호화 키 분리
- **Step 2 (텔레그램 1:1 개인화):** 회원별 고유 Deep-Link 및 개별 알림 라우터 구축
- **Step 3 (마스터 관리자 대시보드):** 대표님 전용 회원 관리 & 라이선스 제어 패널 개발
- **Step 4 (클라우드 배포):** 고정 IP 클라우드 인프라 배포 및 실전 회원 초대 테스트
